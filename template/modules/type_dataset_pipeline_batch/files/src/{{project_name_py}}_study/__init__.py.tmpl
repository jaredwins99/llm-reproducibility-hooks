"""The {{project_name}} study pipeline.

Everything specific to this study: loading config/study.yaml, the readers
for its sources, and its rules. Generic, data-agnostic functions belong in
the {{project_name_py}} library package, which this package imports and which
never imports this one. The stage order lives in project.mk (or the Makefile).
"""
