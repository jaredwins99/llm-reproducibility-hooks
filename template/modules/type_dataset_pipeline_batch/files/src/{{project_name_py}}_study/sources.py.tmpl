"""Readers for this study's raw sources.

Study-specific: each reader knows the layout of one export. Swap
read_source for the real reader; keep it returning a DataFrame.
"""

import pandas as pd


def read_source(path):
    """One raw CSV export, every column as text and no NA inference."""
    return pd.read_csv(path, dtype=str, keep_default_na=False)
