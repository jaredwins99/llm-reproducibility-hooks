# Study configuration for {{project_name}}. Paths are relative to the
# repository root; stages read them through {{project_name_py}}_study.config.
paths:
  raw: data/raw/example.csv
  extracted: data/01_extracted/example.parquet
  transformed: data/02_transformed/example.parquet
  output: outputs/example.csv
